# Raven2 baseline integrity and kernel attribution handoff

The operator keeps `lease-q4k-6b262d93-r1` serving. The device window restores
that release and its authenticated LAN boundary. The active and previous
bundles, standing VM, held sweep and frozen binary remain retained. Repository
source integration supplies future scripts; a newer source commit alone calls
for neither a release rebuild nor activation.

## Integration observations

Main at the integration check before this handoff revision is
`b87df5a7ff0973c1f0b0a421524eccd021db677b`. The stage-timing implementation
(#211), baseline harness (#216), MTP attribution implementation (#217,
Program 1), baseline integrity repair (#218), and CI environment repair (#219)
are merged implementation.

Program 2's original published branch remains at
`6cb1b9add2aecd31aade1ff652dfdd41244fcf94`. Its gate exited 143 and its driver
ended at a rebase conflict in `evidence/SHA256SUMS`; the original branch and
interrupted worktree remain preserved. The corrected Q6_K content included in
this revision is owned by [PR #220](https://github.com/Oichkatzelesfrettschen/qwen-apu/pull/220),
through source commit `0fdea244`. The correction checks finite numbers, complete
raw/exclusive totals, interval bounds, unique pipeline IDs, quantile ordering
and stored-source formats. Reprocessing preserves the eight retained ledgers'
family/member numeric results. The PR's final exact-head gate receipt and GitHub
MERGED record establish its main landing; the old PID and published original
branch supply neither. Content integration leaves the original branch ancestry
and interrupted worktree disposition separate.

The repository is public following operator authorization. Hosted CI resumed,
exposed missing tools and Ubuntu sandbox-path assumptions, and passed all 145
cells for PR #219. That merged repair installs the runner dependencies, resolves
actual merged-/usr paths and temporary-venv mount order, and preserves the
grader's filesystem/network isolation. Source integration requires no deployment
activation; the appliance runtime manifest still identifies `bb7f5a2a` from the
canary window.

The first 2B canary completed its streamed warmup and stopped at instrument
admission, before any repeated decode request. The failure and authenticated
service restoration are retained below. The 0.8B/4B anchors, speculative
verification attribution, Q6_K candidates and remaining product measurements
remain unrun. Further device windows are held after the infrastructure failure.

The original two untracked handoff files have byte-verified preservation copies
outside the checkout. Their original SHA-256 values are:

| File | Original SHA-256 |
| --- | --- |
| agent-preamble-20260907.md | e7274e971a4a2baa79141e66db532397989c110a8a9dc2d2235559c25f427997 |
| handoff-20260907.md | 2c671e170dd1628b4a8b2109c853f09d4d8dfdce03f2b748055060c57e7487df |

The successor packet's nine probes were reported as source-excerpt replays.
The packet's sandbox download paths are absent on the workstation. The
canonical fixture suite reproduces the pasted refusal requirements against the
actual checkout; that test scope is distinct from the packet and from a device run.

## Baseline repair and first acquisition

PR #218 repaired the existing baseline library, runner, reader and fixtures together.
The `serve-baseline-fixed` lease profile keeps the orchestrator and monitor at
nice 0, preserves KSM as found and the standing VM, selects stock package power,
and fixes SCLK=2/FCLK=2 (1100/933 MHz). Serving policy sets inference priority.
Retain the KSM observation and restoration record as experimental conditions.

The streamed warmup timestamps first content and drains to terminal completion.
Partial events carry token IDs; the terminal event carries the final timings.
Readiness and request deadlines bound elapsed time. Sampler exit status,
source, cadence, priority, affinity and coverage govern instrument admission.
A quiescence failure terminates acquisition before the next arm. Launch, request,
parse, sampler, signal and cleanup failures retain their terminal state.

The reader parses retained requests and responses, recomputes tokens and rates,
checks finite numbers and timing consistency, requires exact ordered arm and
repeat populations, and checks actual process and runtime identities. A
manifest's available variants state capabilities; process selection states the
variant measured. A stable candidate can differ from a stable control.

Server decode timing numerically excludes model loading. Startup can influence
later clocks, caches and temperature. Four repeats in a loaded session measure
within-session variation. C K K C supplies two launches per role; its ratio is
descriptive. A spread above 4% does not identify the cause, and a four-launch
correlation establishes neither failed warmup nor a stopping rule. Keep the
registered repeat count fixed after observing the result.

A future authorized 2B single-mode canary requires the hwmon follow-up repair
to pass focused tests and its exact-head gate before the named window opens. Require one completed warmup, four completed fixed-length repeats,
accepted identity and sampler evidence, bounded teardown, restoration, and final
authenticated health. Stop at the first infrastructure failure. The 0.8B and 4B
anchors advance in separately bounded windows only after that canary passes.
The full thirteen-row portfolio remains queued for a concrete selection decision.

Preserve `.runtime/results/<run-id>` raw records. Retain source and execution
identities for the whole window; sync before it and freeze scripts and deployment
links during it. Promote reviewed sanitized evidence afterward.

## Corrected kernel prerequisites

MTP (#56) asks about Q4_K verification. `q8-mat-vec-receipt.sh` compiles Q8_0;
its column argument remains a separate capability. Use the exact Q4_K module
from the instrumented executable, or a fully matched Q4_K compile, in the general
shader lab at `(BLOCK_SIZE, NUM_ROWS, NUM_COLS)` equal to `(64,1,2)`, `(64,2,2)`
and `(64,4,2)`, with the one-column control retained.

Record realized module, specialization, proposal count, verification width,
acceptance, rejected work, bonus tokens and committed output. A one-token draft
maximum alone establishes no realized target-pass width. Ordinary
`predicted_n - 1` decode transitions and speculative verification rounds have
different populations. Bind a compatible census calibration control separately
from the served baseline, or retain the instrumented comparison's overhead limits.
Measure committed tokens divided by draft, verification and bookkeeping time.
The historical 100--140 ms estimate remains a falsifiable prediction, including
where a new path is substantially faster.

Q6_K (#57) retains its output-projection analysis. PR #220 reconciles the published
analysis with finite/range checks and head-bound gate receipts. Reprocessing covers
eight retained ledgers: four 2B, three 4B and one Q8 comparison; numeric outputs
agree. The empty 4B 22-I1 ledger remains refused. The original fourteen figure
counts Q4/Q6 family rows from seven acquisitions, rather than fourteen arms.
Retain acquisition and analysis revisions separately.
The 62,080/83,840 ratio is about 74% of workgroups. The 23.7--25.4/36.7 ms ratio
is about 65--69% of family time. The 37.0% and 29.3% figures are shares of raw
bracket sums. Sum of pipeline unions bounds the family union from above.
Mixed tensor shapes, unused tensors and file-byte populations constrain any
per-byte comparison; they establish a workload observation rather than intrinsic
format ranking. Group by tensor shape and target the large tied vocabulary
projection first. A mixed-shape p99/median ratio alone identifies no random stall.

## Finite continuation queue

| Task | Next action | Advancement condition |
| --- | --- | --- |
| #68 Q8 | Build host selection for the retained four-row specialization | Actual output-head dispatch changes from 124,160 to 62,080 workgroups; twelve other changed shapes remain accounted affected consumers |
| #21 E5 | Prove consumed OpSDotKHR module and isolated driver; arithmetic, quantization, then combined timing | Earlier rungs pass and quantizer plus consumer wins; E5-M is manual control, S0 generic lowering, S1 target-aware lowering |
| #56 MTP | Corrected Q4_K two-column attribution | Realized pass width, module and whole-round acceptance accounting are established |
| #57 Q6_K | Registered lab candidates, then surviving mechanism timing | Static and executed identities agree; local shortening survives whole-path accounting |
| #27 prefill | Print the admitted plan, then missing rungs | Actual prompt depth, generation headroom, skips, failures and completion remain explicit |
| #20 image / #32 web | Diagnose the specific language-profile and evidence-use failures | Each product's own admission succeeds |

Choose one kernel experiment from its established prerequisite after the anchor
baselines. Keep E5's driver isolated from the appliance's global RADV installation.
Q8 static proof already exists and waits for host selection, not another static run.

Prefill starts with `QWEN_PREFILL_LADDER_PRINT_PLAN=1`. The default 16 generated
plus 32 reserve tokens and one slack token prevent a literal 32,768-token prompt
inside a 32,768 context. Record actual clamped depth, select GFX/FCLK explicitly,
and leave skipped deep rungs unmeasured. Row-specific evidence can admit row-specific
settings; a shared default requires the doctrine's additional cross-class evidence.

Stage timing (#54) uses the actual authenticated router origin and explicit model
IDs. Add bounded curl request timeouts before running twenty requests. The metric
is first response byte of an unstreamed one-token reply. Twenty samples provide
descriptive order statistics, with direction, initial residency, cold/warm state,
timeouts and errors retained. Reserve quiet service use and restore the intended
resident model; an initial request may already name the resident child.

Web retains 0.8B `web-lookup` success separately from 2B `web-reader` failure.
Promote through registry, presets and bundle admission. Two web sections leave
residency policy unchanged. Image uses the intended 4B proposal profile and retains
the reviewer's complete response, finish reason, usage and schema result. Loading,
generation, review and publication remain separate timing phases.

## Retention and gate work stays off the device critical path

Search declared text producers and configurations for synthetic-corpus consumers.
Record selective intentional-discard intent while preserving the sweep hold and
frozen binary. `check-deletion-plan.sh` validates plan, verdict, fingerprint and
payload-manifest operations; it writes no decision record. Its uninstall and
purge actions select runtime-wide plans. DO NOT turn selective corpus disposal
into either action. Any later selective removal needs a reviewed operation naming
the exact intended path.

Gate precision (#75) preserves both mutation directions: unrelated cell registration
keeps an unchanged cell reusable; a changed shared validator invalidates dependents.
Keep immutable cache reuse separate from mutable test state, retain existing
terminal driver outcomes, and serialize fixture-heavy integration.

## Observed canary stop and workstation repair

PR #218 landed the baseline integrity repair, preserved handoff and corrected
Q4_K MTP lab instructions at `bb7f5a2a`. Its exact-head local gate accepted all
145 cells at `c8914949`. The one 2B window
`baseline-2b-single-20260908T0157Z` completed streamed warmup and stopped before
repeats because the broker received no hwmon path. Sampler exit 0 and validator
exit 1 remain separate in the failed acquisition. The window restored service,
KSM and the clock policy; authenticated health passed and the lease executable
and bundle links held. The 0.8B/4B anchors and kernel windows remain unrun.

The workstation follow-up binds and records the selected DRM device's hwmon
path and exercises the real broker against synthetic sensor files. The retained
failure and its source identities are in
`evidence/kernel-program/baseline-canary-hwmon-refusal/`. Keep the acquisition
and its original reader intact. The repaired head requires focused and exact-head validation before another
canary. Keep anchors and kernel windows held until a 2B canary is admitted;
the failed canary supplies no performance denominator.

## Admitted 2B canary and remaining anchors

Maintenance-lock repair: complete (PR #222). Recovery: complete, including a retained initial bytecode refusal and corrective relaunch. The 2B measurement-path canary passed. The descriptive decode mean of 9.759 tok/s is a within-session observation, not a promotion verdict. The reviewed receipt is `evidence/kernel-program/baseline-2b-single-20260908T074513Z/`. Raw acquisition remains private under `.runtime/results/`.

Post-merge CI run 34200444641 passed a5224312. Serving deployment remains lease-q4k-6b262d93-r1. The 0.8B anchor is pending, followed by the 4B anchor in a separate restored window. The operator authorized these two anchors and then the actual four-row Q8 candidate; other device campaigns remain held.

The authorized 0.8B attempt `baseline-08b-single-20260908T141905Z` refused tuple admission after readiness: served context 24576, registered context 8192. Warmup and repeats remained unrun. Runtime-manifest and bundle/KSM comparisons passed; final authenticated health passed (200/401/200). Private raw records remain under `.runtime/results/`. The 4B anchor and Q8 device comparison stay held after this first infrastructure failure. The released 4B formulation is `e4-scale-licm/4`, verified from the serving bundle policy.

## Completed class anchors and Q8 candidate preparation

The repaired 0.8B acquisition `baseline-08b-single-20260908T150028Z` passed at context 8192 with a descriptive decode mean of 16.164009 tok/s. The subsequent 4B acquisition `baseline-4b-single-20260908T150537Z` passed at its registered tuple and observed `e4-scale-licm/4` selection, with a descriptive mean of 3.426340 tok/s. Both completed the streamed 64-token warmup, four fixed-length repeats, token repeatability, telemetry admission, teardown, restoration, snapshot comparison and authenticated health. Their sanitized receipts are under the matching `evidence/kernel-program/` directories; private raw acquisitions and source archives remain under `.runtime/results/`.

The passed 2B canary remains 9.759316 tok/s and was not repeated. These three rates are within-session observations, not promotion decisions. Measurement source aaf8d0e0 passed the exact-head local gate and hosted CI, then merged through PR #223 as 85104729 with an identical Git tree. The serving deployment remains lease-q4k-6b262d93-r1 and executable 510c0420... .

The workstation Q8 candidate is built separately as c9248c06d1e53cbef8856ae31753725b8d6d1f8d0726cb96425ffd2f964d685e. The repository base-build identity helper matches the frozen control and candidate. Both Q8 modules match the retained static-proof digests. The host-expression and shared-series admission fixtures pass; actual executed specialization, compatible instrument calibration, local-envelope timing, whole-graph timing and numerical comparison remain pending. Other device lanes remain held.
