# Image-quality successor stopped by the second image grant

## Result

Authorization `image-quality-successor-20260910-01` admitted one execution of
the registered successor contract. The accepted v2 preflight bound the declared
browser environment, the complete `marionette_driver` distribution manifest,
the top-level acquisition controller, its retained edge driver, and Firefox
before browser startup. One disposable headless profile then contacted the
already-qualified numeric appliance origin.

The fox row completed generation and review. Direct inspection passed two of
five predefined constraints: the image contains one fox and no other animal or
foreground object. The image uses a frontal or three-quarter view instead of a
strict left-facing side profile, its body and tail merge into the background,
and four plausible legs are absent. The registered reviewer returned valid JSON
and passed its single `prompt_subject` constraint while also requesting a
composition change and regeneration. That judgment disagrees with the direct
constraint inspection. The no-reroll rule suppressed the requested
regeneration.

The cube row reached an exact registered proposal and application approval.
The broker then refused generation because the client still held one
outstanding image grant. That authorization failure activated the stopping
rule. The cube produced no image, and the exact-text row remained `not_run`.
The acquisition consumed and closed its single-use authorization; grant expiry
does not authorize a retry.

## Row outcomes

| Row | Seed | Protocol | Generation | Artifact | Latency | Reviewer | Direct inspection | Agreement |
| --- | ---: | --- | --- | --- | ---: | --- | --- | --- |
| Fox side profile | `314159` | `completed` | `completed` | `82a618aa4acfa3d16fe3885c60b2e633911a2d8aff9d690c4f8df978f5159afb` | 33.111 s total | completed; schema valid; subject passed | 2/5 constraints passed | false |
| Red cube left of blue sphere | `271828` | `authorization_failure` | `not_started` | `not_produced` | `not_measured` | `not_run` | `not_run` | `not_assessable` |
| Exact-text sign | `161803` | `not_run` | `not_run` | `not_produced` | `not_measured` | `not_run` | `not_run` | `not_assessable` |

The fox provenance reports 33.111 seconds total and 33.073 seconds in the image
runtime. The retained provenance exposes no component timings for model load,
text encoding, diffusion, VAE, or PNG encoding, so those fields remain
unavailable rather than inferred.

## Protocol and retention

The registered contract SHA-256 is
`8ace8d10ff829e93d143b3376397b53fa633606feb0bd8b2a934d0c2a371bf09`.
The acquisition controller SHA-256 is
`6c7ca29409f5663b2c615f22b76cd65d27bc61df762d10add288a8129b49af19`;
the edge-driver SHA-256 is
`f6e530f5e3fe9c2505f9d33b0b6f576d0fccf423bded4659ac87bea1ecf12a25`.
The v2 preflight recorded Python 3.14.7, `marionette_driver` 3.7.1, and the
complete distribution-manifest digest
`b9375040ae3c86961f3250bc1b257f3809642097be453dfc4d9bd752a67a384b`.

The private acquisition remains under the runtime results root. It retains the
PNG bytes, provenance, browser events, raw reviewer response, approval text,
session receipt, credential input, screenshots, lifecycle terminals, and
before-and-after observations. `result.tsv` is the compact reviewed source
derivative. `transformation.tsv` binds the private summary digest, sanitizer
digest, public derivative digest, and substitution count. The PNG and private
browser records are absent from Git.

The predecessor acquisition remains closed and unchanged. Its first-row
terminal still hashes to
`bca71a86cac229c44d094e4cc5b24311d581d43c4072ebc9a00e3bc2d19ce0fb`;
its states remain `failed_before_start`, `not_run`, and `not_run`.

## Cleanup and preservation

Firefox cleanup completed, the disposable profile was removed, the Marionette
port closed, and every acquisition-owned browser process exited. The following
before-and-after observations matched:

- runtime source-manifest SHA-256
  `cae4b6220983880e188595eae842f30fc47cadbaf55e8e23b175a1bcebcd9293`;
- served-page SHA-256
  `4539f12f10cd2627383c3bd3ece520273061c7954ae40205668de0d5a8c6a891`;
- deployment-receipt SHA-256
  `82374f61c9dc7cbf142215e8584c3047082c431d29a3c95346c54579276f1651`;
- serving-executable SHA-256
  `510c0420346ffa4f5104d3f2b28117262a0d30b2907dc21c833d38442c3e49af`;
- seven owned service process identifiers and start identities;
- authenticated router, broker, and artifact results `200`, `200`, and `200`;
- unauthenticated router, broker, and artifact refusals `401`, `403`, and `401`;
- KSM run state `1`, DPM policy `auto`, and the exact QEMU process identity.

The preservation claim covers those enumerated observations only. Filesystem,
network, kernel, VM guest, and application state outside them remain
unmeasured. The image authorization covered no Q8 prerequisite inspection,
replacement calibration, or candidate timing.
