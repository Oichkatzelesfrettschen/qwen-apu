# Q8 four-row host selection

The candidate `llama-vulkan-q8-four-row-select.patch` selects four rows for the Q8_0 floating-input mat-vec path on AMD GCN. One `q8_rows` value reaches both the pipeline workgroup denominator and specialization constant 1. Other quantization families retain their existing selection. All affected Q8 shapes remain affected consumers; non-Q8 paths are controls.

The patch applies after the production series alone and after the released lease bundle's six Q4_K candidate members. The shader source remains unchanged. The static rows-2/rows-4 receipt in `../raven2-vulkan-kernel-census/q8-num-rows-20260907T1405Z/` remains a source to compare against the candidate build's actual module; unchanged shader text alone does not establish compiler or module identity.

`python3 remote/test-q8-four-row-select.py` compiles the host expressions extracted from the patch with fatal compiler warnings. The fixture checks vendor/architecture selection, column specialization, and agreement between row count and dispatch denominator. The output-head geometry predicts 124160 control workgroups and 62080 candidate workgroups for 248320 rows. The fixture is a host-expression replay, not an executed Vulkan pipeline or served-model measurement.

Instrumented builds, actual module and dispatch observation, applicable census calibration, local envelope and whole-graph timing, and numerical comparison remain pending. The served comparison must bind the released Q4_K candidate series plus exactly this Q8 delta; the two baseline anchors are separate prerequisites and supply neither instrument calibration nor a promotion interval. The release binary and deployment remain unchanged.

The binary comparison accepts `QWEN_AB_SHARED_CANDIDATE_SERIES` as an explicit comma-separated registration of the released shared members, in patch-ledger order. The default remains `-`. The runner validates membership and order, forbids the tested delta inside the shared set, adds the census member itself in kernel-delta mode, and requires both manifest series to match the resulting one-patch difference. A shared control's series digest must match the retained patch bytes. Keyed same-binary comparisons refuse this additional registration.

`test-ab-shared-series.py` checks the empty and released-series cases plus duplicate, unknown, reordered, and overlapping-delta refusals. `test-run-served-binary-ab.sh` drives actual preflight through a registered shared pair, an unregistered pair refusal, and a stale shared-control digest refusal. The source-level host fixture and these admission fixtures establish neither delivered GPU time nor instrument calibration.

The separate workstation serving-shaped candidate has SHA256 `c9248c06d1e53cbef8856ae31753725b8d6d1f8d0726cb96425ffd2f964d685e`. `census_base_build_identity` yields SHA256 `90eec9d593aaaf69990b2060d2b386ae72fea558ddc4e4226b3f25e90e6b7c38` for both the frozen control and candidate. GCC reports Ubuntu 13.3.0-6ubuntu2~24.04.1. The generated plain and subgroup Q8 SPIR-V modules match the retained static-proof hashes `fb422b01...` and `8b5ddf19...`. These checks preserve the static receipt; they do not establish runtime pipeline selection. Private build inputs, package inventory, complete manifests and logs remain under `.runtime/results/q8-four-row-selection-preparation/`. Initial container Git-configuration warnings remain retained; a corrected invocation disabled unsuitable Git performance caches and revalidated the same candidate without warning/error matches in its log.
