# Completed qwen38-4b-distill anchor

Acquisition `baseline-4b-single-20260908T150537Z` completed one streamed 64-token warmup and four fixed-length repeats. The descriptive within-session decode mean is 3.426340 tokens/s. Within-binary repeatability held, acquisition completeness completed, and instrument admission accepted. Cross-binary comparison is not applicable. The observed formulation is e4-scale-licm/4; the 4B process binding reads the selected variant rather than the compiled variant inventory.

All window, runtime-manifest comparison, bundle/KSM comparison, snapshot comparison, and authenticated-health terminal statuses are zero. The snapshots match over executable path and digest, current/previous bundle targets, API-key hash, VM start identity, KSM and DPM policy. Serving deployment remains lease-q4k-6b262d93-r1 with executable SHA256 510c0420346ffa4f5104d3f2b28117262a0d30b2907dc21c833d38442c3e49af.

The private raw acquisition, source archives, actual argv, responses and telemetry remain under `.runtime/results/baseline-4b-single-20260908T150537Z/`. The selected files are bound by selection.tsv; transformation.tsv records the retention substitutions and derivative hashes. The sanitizer is remote/retain-acquisition.sh at 85104729cee2eaf13f9f7b2a9ca4af5e9eeb13e3. Private topology and full process captures are excluded from the public selection.

The baseline supplies an observed served denominator. It establishes neither a throughput improvement, instrument calibration, nor a promotion interval. The earlier 0.8B tuple-admission refusal remains a separate failed acquisition; the passed 2B canary was not repeated.
