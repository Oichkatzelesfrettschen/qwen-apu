# Fabricated harness output. These files exercise the extraction, the
# statistic reader, and the receipt writer on a host that carries no RADV
# part; every value here is invented and none of it is a measurement of any
# device. The disasm block below names three v_mul_i32_i24_sdwa products and
# two v_add3_u32 accumulations, the encodings PR #2115's target-aware
# soft-sdot lowering emits, so the receipt's counted fields are checked
# against a known answer rather than against another receipt.
device_name=AMD Radeon Graphics (RADV RAVEN2)
driver_name=radv
driver_info=Mesa 26.2.1+git-fixture
device_api_version=1.4.354
subgroup_size_min=64
subgroup_size_max=64
spirv_bytes=6852
spirv_capabilities=Shader,DotProductInput4x8BitPacked
features_enabled=subgroupSizeControl,computeFullSubgroups
robust_buffer_access=off
spec_constants=0:64,1:1,2:1
bindings=5
push_constant_bytes=52
subgroup_size_requested=64
pipeline_created=fixture.spv
environment_names=GGML_VK_LOW_PRIORITY,GGML_VK_MAX_NODES_PER_SUBMIT,RADV_DEBUG,VK_DRIVER_FILES,VK_ICD_FILENAMES
