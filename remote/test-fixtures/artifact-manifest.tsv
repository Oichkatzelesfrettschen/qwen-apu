# fake-llama-server.sh stands in for a promoted build, so it carries the
# declaration a promoted build carries. qwen-capacity-policy.sh reads
# checkpoint_semantics from the manifest beside the executable and refuses a
# positive context checkpoint count against any other value; the refusal itself
# is covered by cases that point the policy at a copy holding no manifest and
# at one declaring forced-tail-v0.
preset	fake-llama-server
commit	fixture
worktree	fixture
checkpoint_semantics	natural-boundary-v1
